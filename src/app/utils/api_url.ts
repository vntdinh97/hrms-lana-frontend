import { environment } from "src/environments/environment";

export const EMPLOYEE_API = `${environment.baseUrl}/emp`;
export const WORK_API = `${environment.baseUrl}/shift`;
export const HOLIDAY = `${environment.baseUrl}/holiday`;

